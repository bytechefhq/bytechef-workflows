label: Hello for-each
    
tasks:          
  - name: each
    type: each/v1
    parameters:
      list: "${range(1,25)}"
      itemVar: item1
      iteratee:
        name: each1
        type: each/v1
        parameters:
          list: "${range(1,25)}"
          itemVar: item2
          iteratee:
              type: sleep
              millis: 1000
    
